{
  "name": "integration-template-aws",
  "version": "1.0.0",
  "scripts": {
    "deploy": "npm install; npm run package; serverless deploy",
    "test": "LAYER_CONFIG=../test/mock/layer_config_test.json sls invoke test"
  },
  "dependencies": {
    "@layerhq/idk": "^1.0.0",
    "common": "file:./common/"
  },
  "devDependencies": {
    "proxyquire": "^1.8.0",
    "serverless-mocha-plugin": "^1.7.0"
  }
}
