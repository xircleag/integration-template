{
  "name": "integration-template-aws",
  "version": "1.0.0",
  "scripts": {
    "deploy": "npm install; serverless deploy",
    "logs": "serverless logs -f webhook -t"
  },
  "dependencies": {
    "@layerhq/idk": "^1.0.0"
  },
  "eslintConfig": {
    "env": {
      "node": true,
      "es6": true
    }
  }
}
