{
  "name": "common",
  "version": "1.0.0",
  "description": "Layer integration template common",
  "scripts": {
    "test": "mocha --require should --bail test/*spec.js"
  },
  "devDependencies": {
    "mocha": "^4.1.0",
    "should": "^13.2.1"
  }
}
